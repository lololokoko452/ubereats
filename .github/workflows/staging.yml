name: Staging Deploy

on:
  push:
    branches:
      - develop

permissions:
  contents: read
  id-token: write

jobs:
  validate:
    name: Pre-deploy validation
    runs-on: ubuntu-latest
    steps:
      - name: Checkout sources
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 18
          cache: npm
          cache-dependency-path: |
            client/package-lock.json
            server/package-lock.json

      - name: Install server dependencies
        working-directory: server
        run: npm ci

      - name: Install client dependencies
        working-directory: client
        run: npm ci

      - name: Lint backend
        working-directory: server
        run: npm run lint

      - name: Test backend
        working-directory: server
        run: npm test

      - name: Lint frontend
        working-directory: client
        run: npm run lint

      - name: Test frontend
        working-directory: client
        run: npm test

      - name: Build frontend
        working-directory: client
        env:
          NODE_ENV: production
          VUE_APP_API_URL: ${{ secrets.STAGING_API_URL }}
        run: npm run build

  deploy:
    name: Deploy to staging
    runs-on: ubuntu-latest
    needs: validate
    environment:
      name: staging
    steps:
      - name: Checkout sources
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 18
          cache: npm
          cache-dependency-path: |
            client/package-lock.json
            server/package-lock.json

      - name: Install server dependencies
        working-directory: server
        run: npm ci

      - name: Install client dependencies
        working-directory: client
        run: npm ci

      - name: Prepare backend
        working-directory: server
        env:
          NODE_ENV: production
          JWT_SECRET: ${{ secrets.STAGING_JWT_SECRET }}
          MONGO_URI: ${{ secrets.STAGING_MONGO_URI }}
        run: |
          echo "Preparing backend for staging"

      - name: Run database migrations
        working-directory: server
        env:
          NODE_ENV: production
          MONGO_URI: ${{ secrets.STAGING_MONGO_URI }}
          MIGRATION_DRY_RUN: ${{ secrets.MIGRATION_DRY_RUN_STAGING }}
        run: |
          if [ -z "$MIGRATION_DRY_RUN" ]; then
            export MIGRATION_DRY_RUN=true
          fi
          npm run migrate

      - name: Deploy to staging
        run: |
          echo "Deploying frontend and backend to staging..."
          echo "Frontend artifacts available in client/dist"
          echo "Backend ready in server/"

      - name: Post-deploy health check
        env:
          HEALTHCHECK_URL: ${{ secrets.STAGING_HEALTHCHECK_URL }}
        run: |
          if [ -z "$HEALTHCHECK_URL" ]; then
            echo "No health check URL configured. Skipping."
          else
            curl --fail --silent --show-error "$HEALTHCHECK_URL"
            echo "Staging health check succeeded."
          fi

      - name: Success notification
        if: success()
        env:
          SLACK_WEBHOOK_URL: ${{ secrets.SLACK_WEBHOOK_URL }}
        run: |
          echo "::notice ::Staging deployment succeeded on branch ${{ github.ref_name }} (commit ${{ github.sha }})"
          if [ -n "$SLACK_WEBHOOK_URL" ]; then
            curl -X POST -H 'Content-type: application/json' \
              --data "{\"text\":\"[SUCCESS] Staging deployment succeeded for ${GITHUB_REPOSITORY} (${GITHUB_SHA}).\"}" \
              "$SLACK_WEBHOOK_URL"
          else
            echo "Slack webhook not configured; skipping notification."
          fi

      - name: Failure notification
        if: failure()
        env:
          SLACK_WEBHOOK_URL: ${{ secrets.SLACK_WEBHOOK_URL }}
        run: |
          echo "::error ::Staging deployment failed on branch ${{ github.ref_name }} (commit ${{ github.sha }})"
          if [ -n "$SLACK_WEBHOOK_URL" ]; then
            curl -X POST -H 'Content-type: application/json' \
              --data "{\"text\":\"[FAILURE] Staging deployment failed for ${GITHUB_REPOSITORY} (${GITHUB_SHA}). Check workflow logs.\"}" \
              "$SLACK_WEBHOOK_URL"
          else
            echo "Slack webhook not configured; skipping notification."
          fi
